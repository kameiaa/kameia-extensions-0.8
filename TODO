Bugs:
- Random can't load one page
    Happens on random, logcat the error in app,
    investigate why random pages don't load and fail instantly

Features:
- Persistent search arguments
    Reference MangaDex/nhentai
    SourceStateManager